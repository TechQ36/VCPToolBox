# BackendImageGen 自选模型生图（走自有 API）
# 本插件使用根目录 config.env 中的 API_URL、API_Key，无需在此重复配置。
# 生图模型：优先使用下面的 VarBackendImageModel；若未设置，则使用根目录的 WhitelistImageModel 中的第一个模型。

# 可选：指定用于生图的模型 ID（与后端支持的图像生成模型一致）
# VarBackendImageModel=gemini-3-pro-image-[星星公益站-反重力渠道]
